name: zaraz-feedback-schema

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Check out trusted main source
        uses: actions/checkout@v7
        with:
          ref: main
      - uses: actions/setup-node@v7
        with:
          node-version: 24
      - name: Probe Zaraz feedback schema only
        env:
          FEEEYE_CLOUDFLARE_ANALYTICS_TOKEN: ${{ secrets.FEEEYE_CLOUDFLARE_ANALYTICS_TOKEN }}
        run: node build/ops/zaraz_feedback_probe.mjs --out ops/automation/working/zaraz-feedback-schema.json
      - name: Upload non-data schema report
        uses: actions/upload-artifact@v7
        with:
          name: zaraz-feedback-schema-${{ github.sha }}-${{ github.run_id }}
          path: ops/automation/working/zaraz-feedback-schema.json
          if-no-files-found: error
          retention-days: 7
